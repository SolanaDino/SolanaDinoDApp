import styled from "@emotion/styled";

export const IncubatorClaimModal = styled.div({
  position: "absolute",
  width: "150px",
  height: "auto",

  "& img": {
    width: "100%",
    height: "auto",
  },
});

export const IncubatorClaimWrapper = styled.div({});

export const IncubatorClaimContainer = styled.div({});

export const IncubatorHoveringEgg = styled.div({});
